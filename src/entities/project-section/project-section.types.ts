import type { Optional } from "../../shared/types/common-type";

export type ProjectProps = { id: number; color: string };
export type ProjectId = Optional<number>;
export type ProjectColor = Optional<string>;
